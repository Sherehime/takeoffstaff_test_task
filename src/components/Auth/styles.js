import makeStyles from "@material-ui/core/styles/makeStyles";

const useStyles = makeStyles((theme) => ({
  auth: {
    border: "1px solid #f55",
    padding: "30px",
    borderRadius:"10%",
    width: 440,
    margin: "calc(40vh - 90px) auto",
  },
}));

export default useStyles;
